# Changelog

## v0.1.0 — Unreleased

Initial scaffolding. See `README.md` for the v0.1 feature list and
`MIGRATING.md` for the FRAS-shaped adoption path.

- Plugin factory `contentTreePlugin(opts)`
- Custom admin view at `opts.adminPath` (default `/tree`)
- Tree built on `react-arborist` (target)
- Right-click context menu (insert/duplicate/rename/delete)
- DnD reorder + reparent (target)
- Deep search with auto-expand (target)
- Optional workflow + lock gutter
- `editUrlBuilder` config for custom right-rail iframe targets
